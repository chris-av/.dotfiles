#/usr/bin/env bash

# dotfiles
tmux new-session -d -s "dotfiles" -n "main" -c ~/Documents/projects
tmux new-session -d -n "nvim" -c ~/.dotfiles/nvim/.config/nvim

# other projects
tmux new-session -d -s "projects" -n "main" -c ~/Documents/projects
